# Dora Studio 交互式用户走查记录

日期：2026-09-16。执行方式：以创作者身份在浏览器页面手动点击、输入、上传测试文件和观察运行画面。该记录是代理扮演用户的交互式走查，不等于真实受邀用户、真实手机或生产部署验收。

## 当前验收结论（2026-09-16 更新）

**受邀预览 MVP 的代理用户整链验收已通过；完整首版验收仍未通过。** 在隔离 HTTPS 前端、API、原 Agent WASM 宿主和独立 Player 中，全新管理员通过正式页面导入 DeepSeek `deepseek-flash`，全新创作者从一句描述得到 r1 游戏，再在同一项目连续完成 r2 障碍与 r3 背景/得分圆点修改。三轮均由原 Agent 调用编辑、`build` 和 `previewGame`；740×340 PNG 已人工复核，源码和图像哈希逐轮变化。成功链 16 笔请求共 147,472 输入、3,325 输出 token，结算 ¥0.321544；含定位失败的全部外部模型调用共 80 笔、¥1.460134，结束时全部 `settled`。

刷新后首次 Agent launch 被注入 503，失败前后没有新增模型请求或金额，重试恢复同一项目；版本记录可见 r0/r1/r2。第二个全新受邀账号读取创作者项目返回 404，自身云项目列表为空。测试使用 Chrome、Player engine build `b9c5e04196e90d922f4bf191873927392fbbf06b7c20cb171e87c61374e52424`，证据在 `apps/web/artifacts/real-mvp/`。这允许进入小规模受邀技术预览，但不替代完整 Agent 逐项等价、BYOK 派发、公开发布/Remix、生产部署、真人签收和真实设备矩阵。

后续验收转入 [受邀预览 MVP](MVP.md) 之后的真人技术预览和完整首版门槛。产品继续修改仍不限制轮次；已验证的两次继续描述只是最小验收样本。当前只能宣布可进入小规模受邀技术预览，不能宣布完整首版通过。

历史正文中“正式页面仍不能从一句描述生成”“试玩能力尚未接通”等句子分别指当时未配置服务的人工环境或旧增量，不能作为当前源码断言。当前正式首页在**受邀账号、获授权共享模型和专用 Agent 宿主均就绪**时才启用描述入口；没有这些服务时禁用是配置门禁，不是入口不存在。作者回写后不由 Studio 自行编译/试玩，只有原 Agent 工具调用或用户手动操作才会触发。

## 环境

- 既有 `http://127.0.0.1:8898/` 正式前端预览：无账号服务和独立游戏运行来源。
- 独立本机验收环境：Studio Vite 前端 `http://127.0.0.1:8950/`，已装配 Dora Web runtime `http://127.0.0.1:8951/index.html`，两者不同来源，使用 `build/studio-runtime` 的 `dora-preset` 引擎。测试后关闭新起服务，既有 8898 预览保持原状。
- 上传文件由 `tests/create-manual-import.mjs` 生成于独立临时目录：一个含 Lua 入口及 `[0,255,128,7]` 资源的 `.dora` 包，一个含 Studio 元数据及 `[1,2,255]` 资源的 ZIP 备份。没有上传用户文件或调用真实模型。

## 创作者路径与结果

| 路径 | 页面操作与可见结果 | 判定 |
| --- | --- | --- |
| 本地新建与保存 | 新建 TypeScript 项目 r0，改动源码，编译成功 r1，保存显示“已保存到本机 · r1”；刷新后从最近项目重新打开，修改后的源码仍在。 | 通过 |
| 编译诊断 | 在副本里输入 `Vec2(;`，页面显示“编译失败”和 1 条 `')' expected.` 问题，点击问题后焦点回到代码编辑区。 | 通过；定位选区未专项核对 |
| 独立副本 | “另存副本”后最近项目从 1 增至 2，副本 r0；在副本里制造错误时原项目仍保持 r1。 | 通过 |
| 未保存切换保护 | 脏草稿切项目时出现“确定放弃这些修改吗？”原生确认框。当前计算机控制无法正常解除该弹窗，未完成取消/同意两侧的交互观察。 | 部分通过 |
| 未配置试玩 | 8898 预览编译成功后，试玩按钮显示“尚未配置独立来源运行服务”，运行游戏禁用。 | 配置缺失时提示符合预期 |
| 浏览器实际试玩 | 在 8950 创建可见 DrawNode 矩形游戏，编译成功，试玩显示绿色矩形，状态“正在运行 · r1”，运行日志出现“人工验收：游戏启动”；停止后实例移除，再启动出现新实例及第二条启动日志。 | 通过 |
| 游戏点击输入 | 尝试直接点击画布及跨来源 iframe canvas，计算机控制都返回点击目标不可用，未观察到 `onTapBegan` 日志。 | 未验收；需实际玩家点击或可控浏览器复核 |
| `.dora` 导入 | 页面上传测试包后出现独立 r0 项目、`init.lua`、声明文件和 4 字节资源；Lua 源码正确。编译出 3 个产物，试玩运行日志出现“人工验收：导入游戏已启动”。资源页能列出二进制文件，未知格式说明暂不支持预览。 | 通过所用轻量 Lua 包，不代表所有原生项目兼容 |
| Studio ZIP 恢复 | 上传带 `.studio/project.json` 的备份后得到第三个独立 r0 项目，源码和 3 字节素材列出；编译 2 个产物，运行日志出现“人工验收：Studio 备份恢复成功”。把 `.dora` 包误送恢复入口时提示“缺少有效的 Studio 备份元数据”，项目数仍为 3。 | 通过所用测试备份 |
| ZIP 下载 | 点击“下载 ZIP”后页面没有错误，但当前浏览器控制在等待下载事件时超时，未取得或解包该次下载文件。 | 未验收；自动化已有另行验证，不替代本次操作 |
| 手机宽度 | 390×844 浏览器视口下最近项目、工作区、Agent 切换可操作；`innerWidth` 与页面 `scrollWidth` 均为 390。 | 视口走查通过；真实手机未验收 |

## 本次发现与修复

资源页原提示“游戏包导入仍待接入”，与已经能导入并运行 `.dora` 的实际行为冲突；云端提示仍称“云项目恢复界面仍在开发”。已更新正式前端文案，明确从左侧导入 `.dora`/ZIP，受邀账号可从“我的云项目”取回，页脚区分 AI 创作待接入和云服务所需配置。Web 生产构建通过，并在验收页面确认新提示出现。

## 首版产品准入结论

本地编辑、编译、浏览器运行和两种导入在本机测试项目上已形成可操作闭环；当前正式页面仍不能从一句描述生成游戏，Agent 输入和发送禁用。账号读取失败时模型用量入口禁用，因此共享 API 金额额度、BYOK 用量、真实登录/邀请、云保存的受邀用户操作都无法在本次环境里人工验收。发布、公众分享、源码开放与 Remix 也没有可完成的正式用户路径。

首版整体验收 **未通过**。下一轮应提供包含受邀登录、真实或受控模型网关、发布服务和独立运行来源的可复现部署，再由受邀创作者按主旅程实际操作；测试账户和共享 API 额度必须与生产用户数据隔离。检查点确认框、画布玩家点击和 ZIP 下载这三项也需用能完成操作的浏览器补验。

## 同日后续自动化增量（不改写以上人工走查结论）

后续已实现首版选定的“邀请码＋账号密码”登录，以及首页选共享授权、云上传、专用宿主原 `Session.sendPrompt` 投递和受保护的共享模型路由。在隔离临时数据库与本地 HTTPS 供应商桩下，四服务 Chrome 脚本模拟管理员邀请/额度配置与创作者一句描述，确认原 Agent 一次模型请求和 SQLite 一笔实际 usage 人民币结算；另一浏览器专项验证云 PUT 503 后同项目续接与项目切回不重发。完整构建/单测 332/332 通过。这是模拟用户的自动化接线证据，不是上面那次人工操作的补签，也不是外部模型生成可运行游戏。自动写回/编译试玩、BYOK、发布/Remix、生产部署和真实受邀用户验收仍未通过；首版整体验收结论保持 **未通过**。

后续长任务安全专项另在隔离临时数据库的 HTTPS 前端/API/Agent 宿主同时运行 Chrome：受邀账号连接专用宿主后实际收到启动续期 200，刷新账号仍连接；退出时 iframe 关闭，旧私有配置 403。创作者模型候选的部署端点/密文 Key/账号额度门禁通过服务端专项，完整构建/单测更新为 334/334。续期没有通过人为等待五分钟的浏览器时钟验证；服务端可控时钟单测覆盖期限及不可复活。仍不改变首版整体验收 **未通过** 的结论。

后续原 Agent 编辑工具专项在隔离的四个 HTTPS 服务下模拟受邀创作者主旅程：本地固定模型回复调用原 `edit_file`，真实 WASM 宿主修改 `main.ts`，正式工作台在主任务结束后自动核对并保存作者 r1，页面明确仍为“尚未编译”，没有试玩 iframe。Chrome IndexedDB 证实回写一次性收据跨重开与错任务/版本拒绝；完整构建/单测 335/335。按用户确认，这里故意不由 Studio 额外编译或运行；下一验收需由原 Agent `build`/`previewGame` 工具在浏览器实际触发并回传结果。模型桩不是外部 AI，正式游戏试玩与首版整体验收仍 **未通过**。

继续以隔离的 HTTPS 前端/API/专用 Agent 宿主/本地模型桩四服务模拟创作者，从输入描述、原 Agent `edit_file` 改写绘图代码到原 Agent `build` 主动发起宿主浏览器 TS Worker：桩收到 `build.success=true` 的工具结果，侧栏显示原执行步骤完成，源码/生成 Lua 接收为作者 r1。工作台仍“尚未编译”且没有试玩 iframe，符合“不要做额外自动编译”的最新确认。完整构建/单测 338/338，Chrome Worker 同名 Lua 重新生成专项通过。此为原工具触发构建的自动浏览器验收，不证明原 `previewGame` 已实际试玩、外部模型/真人使用或完整 Agent 能力；首版整体验收继续 **未通过**。

同一类隔离四服务 Chrome 主旅程进一步让本地模型桩调用原 `execute_command` Lua 模式，输出 `STUDIO_FIXTURE_COMMAND_OK`、工具成功结果和侧栏“已完成”均实际观察到；前端仍没有自行编译或试玩。专用宿主只提供非渲染命令需要的受信入口适配，明确不运行用户游戏。综合 `agent-host-callback.browser.mjs` 压力专项本次出现 WASM SQLite 文本解压内存越界，不能记为通过；原 `previewGame` 独立 Player、非模拟游戏、外部模型及受邀真人验收仍缺，首版结论 **未通过**。

后续五服务 HTTPS Chrome 自动化模拟受邀创作者，让本地固定模型桩按原 Agent 流程调用 `edit_file → build → execute_command(previewGame)`。独立 Web Player 实际运行所生成的游戏，原工具得到一帧 `.agent/vision` PNG 与视觉预算记录；截图中可见绿色游戏绘图，740×340 解码像素核查超过 10,000 个绿色像素。正式页面作者回写后仍显示“尚未编译”，无 Studio 额外编译或持续试玩。该项通过的是原工具触发的浏览器试玩链路，不是本人手动点击走查、真实外部 AI、完整原 Agent 能力或受邀真人签收；首版整体验收仍 **未通过**。

再用两个隔离测试数据库分别模拟 Teal 和 Lua 作者：管理员发邀请码、设置共享 API/账号额度，创作者输入描述，本地固定模型桩让原 Agent `edit_file` 新建 `main.tl` 或 `main.lua`，随后原 Agent `build` 工具实际调用独立宿主 Worker。浏览器检查 Teal 相邻 Lua 以 `-- [tl]: main.tl` 开头、Lua 原文保持不变；供应商桩收到两次各自成功的原 `build` 结果，作者回写 r1 后两次均“尚未编译”、无 Studio 额外运行，金额账本正常结算。这里只验收了普通 Lua/Teal 构建工具链，TIC80、Yue/XML/Yarn、真实 AI、真人签收及首版整体验收仍 **未通过**。

无效 Lua 失败路径再用第三个隔离数据库走同一受邀账号主旅程：原 Agent 创建缺少闭括号的 `main.lua`，原 `build` 返回 `success=false`，模型工具消息含 `line 2, col 0` 与原 Lua 解析错误；作者源码未被构建工具改写，Studio 未追加编译/运行。首次测试桩只读取顶层 `message/messages` 得到 `{}`，复查完整原 batch `results[]` 后确认是桩统计错误，不是诊断丢失。失败路径自动化通过，但并不等于 Agent 会自主修复或真人完成验收。

新增三个隔离空数据库的自动化浏览器主旅程仍从邀请码、共享额度、创作者描述进入原 Agent：固定模型桩让 `edit_file → build` 分别处理 TIC80 Teal、合法 Yarn、非法 Yarn。浏览器读取 Agent 文件系统核实 TIC80 生成 Lua 的前两行为 `-- tic80`、`-- [tl]: main.tl`；合法 Yarn 不产生 Lua；非法 Yarn 的原工具 batch 消息向模型返回 `success=false`、`line 1, col 1`、缺少 `title:` 的原 YarnFlow 诊断。三轮作者回写后工作台均“尚未编译”、无 Studio 额外试玩，人民币用量正常结算。原编译核心及工具边界专项、完整构建/单测 356/356 通过。上述仍是本地固定回复模拟用户，不替代外部 AI、真实受邀用户或完整产品验收；首版结论 **未通过**。

最终受信包上另用新空数据库重跑严格的无效 Yarn 原诊断断言，并让原 Agent 再走 `edit_file → build → previewGame` 到独立 Player；PNG 解码与游戏绿色像素核查、回写后“尚未编译/无常驻 iframe”仍通过。长会话综合 Chrome 脚本也 `passed=true`、`errors=[]`。这些回归不改变外部模型/真人及首版整体验收 **未通过** 的结论。

随后以三个全新隔离数据库和五个同时运行的 HTTPS 服务，继续在 Chrome 中扮演受邀创作者：从一句描述建立项目，本地固定模型桩让原 Agent `edit_file → build` 分别处理普通 Yue、缺闭括号的 Yue、TIC80 Yue。合法 `main.yue` 生成 `-- [yue]: main.yue` 开头的相邻 Lua；错误脚本的原工具返回失败诊断且无目标 Lua；TIC80 产物以 `-- tic80` 与 Yue 来源头开头。最终受信包上第四个新数据库的普通 Yue 主链与长会话综合 Chrome 均复跑通过。四轮共享额度账本结算、作者 r1 自动核对后工作台仍“尚未编译”、无试玩 iframe，说明没有额外自动编译/试玩。完整构建/单测 357/357、Lua 语法与回调专项检查通过。这仍是受控模型工具回复，不等于真实外部 AI、复杂项目、受邀真人或首版整体验收；结论仍 **未通过**。

继续用 Dora 原 XML 转换器的专用 Web Agent 引擎，启动五个独立 HTTPS 服务和三个空数据库，在 Chrome 中模拟受邀创作者的一句描述主旅程。固定模型桩让原 Agent `edit_file → build` 处理合法 `main.xml` 时，宿主真实生成 `-- [xml]: main.xml` 开头且含测试节点名称的相邻 Lua；不匹配闭合标签时，原 `build` 失败诊断进入模型工具消息、没有目标 Lua。强化生成内容断言后第三个新数据库的合法 XML 复跑通过。三轮共享额度结算与作者 r1 核对后工作台仍“尚未编译”、无试玩 iframe。完整构建/单测当时为 358/358、新引擎综合 Chrome 回归通过；专用引擎本机工具链版本漂移，发布需锁定版本重建。该轮尚未验收 XML 实际 Player 试玩；后续补验见下文。XML 复杂标签、嵌入 Yue、真实外部模型和受邀真人尚未验收，首版结论仍 **未通过**。

再以三个全新隔离数据库和五服务 HTTPS Chrome 走原 Agent XML 游戏试玩子链：固定模型桩让原工具先创建绿色 DrawNode 的 `main.xml`，再创建从 XML 工厂取得节点并挂到 `Director.ui` 的 `init.lua`；原 `build` 真正生成 Lua 后，原 `previewGame` 返回一帧 `.agent/vision` PNG。最终加强的浏览器脚本核对两次编辑、入口包装、工具反馈、PNG 解码和绿色像素超过 10,000，保存截图人工查看可见游戏绘图。首轮失败是桩的预览统计截短，改用结构化帧列表后两次独立数据库复跑通过。当前源码完整构建/单测 358/358；隔离服务/证书/库已清理。作者回写后仍“尚未编译”、无额外试玩实例。此项补验的是 XML **受控模型回复的工具触发试玩**，不是 XML 全兼容、真实模型自行规划/继续迭代、BYOK、发布/Remix 或真人验收；首版结论仍 **未通过**。

按最新行为边界复核正式 App，已撤去历史遗留的“回写后自动编译”和构建成功后自动启动试玩意图；手动“编译项目”及“运行游戏”仍保留。综合浏览器验收脚本改为回写后先检查“尚未编译”、无试玩 iframe，再显式点击编译与运行，但该脚本仍因上述 WASM 解压越界在此前失败，不构成手动整链通过证据。此次修订后的完整构建/单测 338/338 通过；首版结论不变，仍 **未通过**。

后续恢复综合 Chrome/WASM 脚本：专用宿主引擎将 `decompressText` 的栈上解压块由 64 KiB 改为 8 KiB 并重建后，无编解码预热的 `agent-host-callback.browser.mjs` 连续两次整链通过，`result.json` 记录 `passed=true`、`errors=[]`。正式 App 夹具在受邀账号下连接 Agent，测试故障确认重试只补确认而不重复提交 r10；r10 作者回写后仍“尚未编译”、没有运行 iframe，用户明确点击“编译项目”和“运行游戏”后测试游戏实际绘图、输出日志并接收画布点击，错误源码也需用户明确编译才出现诊断。该测试是自动化模拟用户操作，不是外部模型自动试玩或受邀真人补签；本机专用引擎构建工具链有漂移，发布仍需锁定版本重建。原 `previewGame` 独立 Player 接线、完整原 Agent 逐工具/模式、外部 AI 与首版真人验收仍缺，因此首版结论继续 **未通过**。

最新五服务 HTTPS Chrome 以全新管理员、创作者和隔离数据库再次模拟用户：管理员导入共享桩并授权 ¥5，创作者一句描述得到绿色首轮 r1；侧栏提交“增加雨滴”后，原 Agent 独立调用 `edit_file → build → previewGame`，作者项目成为 r2，独立 Player PNG 的蓝色像素超过 10,000。随后刷新页面并让第一次 Agent launch 返回 503，页面保留同一项目与轮次记录；再次连接恢复原会话，提交“再增加星光”得到黄色 r3，黄色像素超过 10,000。账号人民币已用金额两次继续增加，版本记录可见 r0/r1/r2，页面没有在回写后附加 Studio 自动编译或常驻试玩。当前完整构建/单测 359/359。该项通过持续创作、刷新和可恢复故障的**固定桩工程验收**；真实外部模型尚未选择，MVP 与首版结论仍 **未通过**。
