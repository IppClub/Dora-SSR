// Compatibility exports for existing scripts. All implementation is shared.
export { createTextInputView as createRemixInputView, inputLength, inputSlice, insertInputText, layoutInput } from "Dev/Mobile/TextInput";
