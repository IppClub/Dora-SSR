-- Emits a global event with the given name and arguments to all listeners registered by node:gslot() function.
-- @param eventName (string) The name of the event to emit.
-- @param ... (...any) The data to pass to the global event listeners.
local emit: function(eventName: string, ...: any)

return emit
