local Color = require("Color").Type

-- 通过RGBA颜色通道值创建一个新的`Color`对象。
-- @param r 红色通道值（0-255）。
-- @param g 绿色通道值（0-255）。
-- @param b 蓝色通道值（0-255）。
-- @param a 透明通道值（0-1）。
-- @return (Color) 新的`Color`对象。
local rgba: function(r: number, g: number, b: number, a: number): Color

return rgba
