local ActionDef = require("Action").Def

-- Creates a definition for an action that makes a delay in the animation timeline.
-- @param duration (number) The duration of the delay in seconds.
-- @return (ActionDef) An ActionDef object that represents a delay in the animation timeline.
local delay: function(duration: number): ActionDef

return delay
