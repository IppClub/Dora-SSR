local ActionDef = require("Action").Def

-- Creates a definition for an action that hides a Node.
-- @return (ActionDef) An ActionDef object that can be used to hide a Node.
local hide: function(): ActionDef

return hide
