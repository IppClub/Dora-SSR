{
  // This file is not used in compilation. It is here just for a nice editor experience.
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
	"baseUrl": ".",
    // @docusaurus/tsconfig 3.10 still defines baseUrl; keep TypeScript 6 checking the site while upstream migrates it.
    "ignoreDeprecations": "6.0"
  },
  "exclude": ["node_modules", "build", "static", ".docusaurus"]
}
