import { Prism } from "prism-react-renderer";
import prismYueScript from './yuescript-prism';
import prismTeal from './teal-prism';
import prismLua from './lua-prism';

prismYueScript(Prism);
prismTeal(Prism);
prismLua(Prism);
