import "@site/src/languages/highlight";

# Pass的类对象

**描述：**

&emsp;&emsp;用于创建着色器渲染流程对象的类。

## __call

**类型：** 元方法。

**描述：**

&emsp;&emsp;用于创建一个新的渲染流程对象的元方法。

**签名：**
```tl
metamethod __call: function(self: PassClass, vertShader: string, fragShader: string): Pass
```

**参数：**

| 参数名 | 类型 | 描述 |
| --- | --- | --- |
| vertShader | string | 顶点着色器二进制文件名字符串。 |
| fragShader | string | 片段着色器二进制文件名字符串。着色器二进制文件字符串必须是以下格式之一：	"builtin:" + theBuiltinShaderName	"Shader/compiled_shader_file.bin" |

**返回值：**

| 返回类型 | 描述 |
| --- | --- |
| Pass | 新的着色器渲染流程对象。 |