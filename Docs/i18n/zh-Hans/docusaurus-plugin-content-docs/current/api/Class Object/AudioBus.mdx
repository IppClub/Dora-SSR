# AudioBus的类对象

**描述：**

&emsp;&emsp;用于创建 `AudioBus` 对象的类。

## __call

**类型：** 元方法。

**描述：**

&emsp;&emsp;创建一个新的 `AudioBus` 对象。

**签名：**
```tl
metamethod __call: function(self: AudioBusClass): AudioBus
```

**返回值：**

| 返回类型 | 描述 |
| --- | --- |
| AudioBus | 创建的 `AudioBus` 对象。 |