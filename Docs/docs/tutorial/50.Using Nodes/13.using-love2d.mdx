# Using Love2D Node

Dora SSR can host Love2D 11.5-style projects as scene nodes. Each `LoveNode`
owns an isolated Lua 5.5 state, virtual window surface, event queue, physics
world, and audio bus. The node is rendered into Dora's scene graph, so a Dora
project can place, scale, hide, restart, or run several Love projects without
giving any of them control of the application window or main loop.

This is source and API compatibility for Love projects. It is not LuaJIT ABI
compatibility and it does not embed SDL's application loop.

## Create a LoveNode

Put the Love project under a Dora content search path. A minimal project can use
the normal Love callbacks:

```lua title="LoveGame/main.lua"
local love = require("love")

function love.load()
	message = "Hello from an isolated Love state"
end

function love.update(dt)
	-- Update this Love instance.
end

function love.draw()
	love.graphics.clear(0.05, 0.08, 0.12, 1)
	love.graphics.print(message, 24, 24)
end
```

Create the node from a normal Dora program:

```lua title="Script/init.lua"
local _ENV = Dora

local game = LoveNode("LoveGame/main.lua")
if game then
	game.position = game.position + Vec2(80, 40)
	game.scaleX = 0.75
	game.scaleY = 0.75
	game:addTo(Director.entry)
end
```

For an extensionless relative path, `LoveNode("LoveGame")` searches for an
existing file at that exact path first, followed by `LoveGame.love`,
`LoveGame.zip`, and `LoveGame/main.lua`. Explicit paths with an extension and
absolute paths are used as-is.

`conf.lua`, `main.lua`, and project modules are resolved relative to the Love
source root. A `.love` or `.zip` ZIP package can also be used as a source. Reads,
writes, mounts, module loading, and package staging all go through Dora's
`Content` system; the isolated state cannot use native Lua modules or bypass the
content policy with host filesystem paths.

`game:restart()` closes the old state and all instance-owned resources before
booting a new state. An error or ordinary `love.event.quit()` stops only that
node. `love.event.quit("restart")` performs the same full restart on the current
LoveNode after `love.quit()` returns; returning a truthy value cancels it.

## Editor and language support

Love definitions are opt-in and use the existing Dora compilation pipeline:

- Lua, Teal, and YueScript: `local love = require("love")`
- TypeScript and TSX: `import "love"`

There is no file comment, project profile, special TSX transform, or default
`love` declaration in ordinary Dora files. This lets one project mix Dora and
Love sources without polluting the other files' globals. TypeScript, Teal, and
YueScript errors are mapped back to their original source locations.

The declaration files shipped with the current engine are the authoritative
list of callable functions and object methods. The table below describes the
broader module boundary.

## Compatibility status

The table separates API-surface adaptation from test coverage:

- **API adaptation** is the percentage of public Love 11.5 wrapper methods
  registered by the runtime and matched by both the TypeScript and Teal
  declarations. The audit currently checks 294 Graphics methods and 420 other
  module/object methods. A `100/100` score describes API surface, not identical
  host-window, renderer, audio-device, or filesystem behavior.
- **Best-platform verification (macOS)** uses macOS because it currently has
  the broadest verification coverage. The number is the pass/selected count
  from the pinned official compatibility snapshot executed on macOS. Skips
  remain visible and are not counted as passes. Modules absent from that
  upstream snapshot use Dora's dedicated macOS runtime workflows instead of an
  invented official count.

| Module | API adaptation | Best-platform verification (macOS) | Implemented scope and important limits |
| --- | ---: | ---: | --- |
| Root callbacks | `100/100` | Dedicated workflows | `load`, `update`, `draw`, `quit`, host-driven `run`, and implemented input/contact callbacks. Dora remains the main-loop owner. |
| `love.graphics` | `100/100` (294/294) | `75/107`, 32 explicit skips | Display state including background/default filter/reset/stack depth, rough/smooth line style, miter/bevel/none joins, and wireframe, affine 2D transform apply/replace/point conversion, renderer limits/capabilities, texture types, Love 11.5 image formats, renderer info, per-LoveNode `getStats`, ordering/performance hints (`discard` and `flushBatch`), primitives, ImageData/CompressedImageData-to-Image upload, Image/ArrayImage layer drawing/Quad/Font/ImageFont/Text, Canvas/MRT, Mesh, SpriteBatch, ParticleSystem, hardware instancing, Shader, stencil/depth/cull/scissor intersection/blend, and readback subsets. The macOS Metal suites cover framebuffer-dependent skips separately. Compressed texture support is renderer-dependent, Love 12 `getTextureFormats` is not exposed, and other GLSL `layout` forms, layered/mipmapped Canvas, plus several extended batching paths remain unsupported. |
| `love.window` | `78/100` (29/37) | `28/36`, 8 explicit skips | Per-node virtual surface size, mode updates, pixel conversion, one virtual display, safe-area/orientation/open/visible/focus queries, and instance-local title/vsync/display-sleep requests. The eight host-window actions—close, move, minimize/maximize/restore, attention, icon, and message box—remain intentionally unavailable. |
| `love.event` | `100/100` | `4/6`, 2 infrastructure skips | Per-instance FIFO with `pump`, `poll`, `wait`, `push`, `clear`, and `quit`. Empty `wait` returns immediately to avoid blocking Dora's host thread. Restart requests affect only the current LoveNode. |
| `love.timer` | `100/100` | `6/6` | Host-driven `step`, delta/FPS/average delta, `sleep`, and `getTime`. |
| `love.keyboard` | `100/100` | Dedicated macOS workflow | Complete Love 11.5 nine-method surface, fixed constants, focus routing, text input/edit callbacks, and text-input rectangle projection. Additional physical layouts, OS repeat, and real IME candidate placement remain device-experience checks. |
| `love.mouse` | `100/100` | Dedicated physical macOS workflow | All 18 Love 11.5 module methods and Cursor methods, including position, button state, visibility, grab, relative mode, image/system cursors, click count, movement, wheel, and node-local coordinates. |
| `love.touch` | `100/100` | Dedicated macOS injection workflow | Press/move/release, touch enumeration, position, pressure, and stable lightuserdata IDs. Routed pressure is currently 1. |
| `love.joystick` | `100/100` | Dedicated macOS virtual-device workflow | Complete Love 11.5 module surface and 21-method Joystick object, including mapping and rumble APIs. Physical controller and rumble output remain device certification. |
| `love.filesystem` | `93/100` (55/59) | `26/32`, 6 explicit skips | Content-backed source/save identities, files, directories, require paths, ZIP/FileData mount, `.love`/`.zip` sources, and deprecated queries. Native C require paths and host symlink controls are the four intentional API gaps. |
| `love.audio` | `100/100` | `28/29`, 1 upstream placeholder | Static/stream/Queueable Sources, spatial state, RecordingDevice, direct filters, and named effects use Dora AudioSource, SDL capture, and a documented SoLoud approximation. Physical recording, permissions, hotplug, and subjective output remain device certification. |
| `love.sound` | `100/100` | `4/4` | SoundData sample access; WAV/OGG/MP3/FLAC decoding; Decoder chunking, seek, clone, metadata, and conversion to SoundData. |
| `love.image` | `100/100` | `5/5` | All 17 Love 11.5 ImageData pixel formats, Content-backed decode, PNG/TGA encode, and CompressedImageData APIs. Metal verifies representative compressed mip uploads; support for a compressed format remains renderer-dependent. |
| `love.font` | `100/100` | `7/7` | Image, TrueType, and text BMFont Rasterizers plus GlyphData and graphics Font behavior. AngelCode binary BMFont is outside the target; stb hinting is not pixel-identical to FreeType. |
| `love.data` | `100/100` | `8/8` | ByteData, DataView, CompressedData, encodings, compression, hashes, and Lua pack/unpack. LuaJIT FFI pointers are unavailable. |
| `love.math` | `100/100` | `6/8`, 2 Love 12 skips | RandomGenerator, color/gamma conversion, convex/triangulation, Love 11.5 noise, Transform, and BezierCurve. Love 12-only noise names are outside the target. |
| `love.physics` | `100/100` | `21/28`, 6 upstream placeholders and 1 Love 12 skip | Isolated PlayRho world, all Love 11.5 physics objects, queries/raycast, external ghost topology, and all eleven joint types. Box2D 3 and Love 12 semantics are not target behavior. |
| `love.system` | `100/100` | `6/8`, 2 physical-side-effect skips | OS/CPU/power queries, clipboard, restricted URLs, vibration behavior, and background-music query. Physical vibration remains device certification. |
| `love.thread` | `100/100` | `5/5` | Independent Lua 5.5 worker states, runtime-scoped Channels, blocking/atomic operations, copied values, Content-backed worker I/O, cancellation, and screenshot Channels. |
| `love.video` | `100/100` | `2/2` | Content-backed Ogg/Theora VideoStream, drawable Video, background decoding, and Vorbis/SoLoud synchronization. macOS verifies pause, seek, looping pixels, audio/no-audio paths, and cleanup. |

## Lua compatibility boundary

Love states run Lua 5.5. Source code, current Lua 5.5 bytecode, `unpack`,
`loadstring`, `package.loaders`, and the 32-bit `bit` compatibility module are
supported. `getfenv` and `setfenv` have limited source compatibility for Lua
functions and positive stack levels. `getfenv(0)` returns the isolated state's
global table, while replacing a Lua 5.1-style thread environment with
`setfenv(0, env)` is not supported by Lua 5.5 and produces a targeted error.
LuaJIT `ffi`, `jit`, LuaJIT/Lua 5.1 bytecode, and Lua 5.1 ABI native modules are
also rejected with targeted diagnostics. Supply source code or compile bytecode
with the Lua 5.5 version shipped by Dora.

## Current platform evidence

macOS is currently the best-verified platform. Its current-source arm64 build
passes the complete 36-workflow Love/Dora integration suite, including Runtime
and sanitizer tests, Metal pixels, SoLoud audio, PlayRho physics, physical mouse,
virtual joystick, multi-instance isolation, reload, Thread, and Video.

iOS Simulator has a universal arm64/x86_64 build and live Metal evidence for
dual LoveNodes, Canvas readback, the 13-scene advanced graphics matrix, the full
PhysicsScene, two Thread generations across restart, and Ogg/Theora Video.
Android has a three-ABI package and Android 14 / API 34 AVD runtime evidence for
the equivalent graphics, physics, Thread, and Video workflows, plus injected
single/multi-touch, keyboard, and explicit IME routing.

Linux ARM64 is built from the current source and starts through Dora's normal
`Script.Dev.Entry` under Ubuntu 26.04. Its Mesa llvmpipe OpenGL ES 3.0 run passes
Mouse/Cursor, Canvas readback, PhysicsScene, the 13-scene graphics matrix, two
isolated LoveNodes, Thread restart, and Ogg/Theora Video. Xvfb and SDL dummy
audio do not prove physical input, a real display driver, or audible output.

Windows 11 now has native MSVC Runtime/PlayRho/SoLoud tests, a complete 32-bit
Dora link, and interactive Direct3D runtime evidence for Thread, compressed
images, Video, advanced graphics, non-2D samplers, PhysicsScene, dual LoveNodes,
audio lifecycle, and system policies. Physical input, microphone, and audible
output on Windows/Linux/mobile devices remain release-certification items and
are not included in the macOS ratio above.
