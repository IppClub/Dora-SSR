# Surface3D

**Description:**

&emsp;&emsp;Displays a 2D Node subtree in a View3D scene.
The engine automatically chooses direct rendering when the subtree can share
the 3D depth buffer, otherwise it uses an isolated render target.