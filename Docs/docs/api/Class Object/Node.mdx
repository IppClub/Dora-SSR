# Node Class

**Description:**

&emsp;&emsp;A class object for the `Node` class.

## __call

**Type:** Metamethod.

**Description:**

&emsp;&emsp;Creates a new instance of the `Node` class.

**Signature:**
```tl
metamethod __call: function(self: NodeClass): Node
```

**Usage:**
```tl
local Node = require("Node")
local node = Node()
```

**Returns:**

| Return Type | Description |
| --- | --- |
| Node | A new instance of the `Node` class. |