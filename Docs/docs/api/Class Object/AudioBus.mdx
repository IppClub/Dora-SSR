# AudioBus Class

**Description:**

&emsp;&emsp;A record for creating `AudioBus` objects.

## __call

**Type:** Metamethod.

**Description:**

&emsp;&emsp;Creates a new `AudioBus` object.

**Signature:**
```tl
metamethod __call: function(self: AudioBusClass): AudioBus
```

**Returns:**

| Return Type | Description |
| --- | --- |
| AudioBus | The created `AudioBus` object. |