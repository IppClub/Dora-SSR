# Dora LOVE Backends

Dora-owned replacements for LOVE platform boundaries belong here. Backend
implementations must not reintroduce an SDL application loop or window
presentation path. Mutable backend state is owned by one `LoveRuntime`.
