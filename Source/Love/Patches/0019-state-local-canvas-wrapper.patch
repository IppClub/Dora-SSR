diff --git a/src/modules/graphics/wrap_Canvas.h b/src/modules/graphics/wrap_Canvas.h
index 257df87e8..345a8a728 100644
--- a/src/modules/graphics/wrap_Canvas.h
+++ b/src/modules/graphics/wrap_Canvas.h
@@ -32,6 +32,15 @@ namespace graphics
 
 //see Canvas.h
 Canvas *luax_checkcanvas(lua_State *L, int idx);
+
+class GraphicsCanvasCommand
+{
+public:
+	virtual ~GraphicsCanvasCommand() = default;
+	virtual void renderTo(lua_State *L, Canvas *canvas, int slice,
+		int functionIndex) = 0;
+};
+
 extern "C" int luaopen_canvas(lua_State *L);
 
 } // graphics
diff --git a/src/modules/graphics/wrap_Canvas.cpp b/src/modules/graphics/wrap_Canvas.cpp
index c4bc7aa64..968ba8efd 100644
--- a/src/modules/graphics/wrap_Canvas.cpp
+++ b/src/modules/graphics/wrap_Canvas.cpp
@@ -19,7 +19,6 @@
  **/
 
 #include "wrap_Canvas.h"
-#include "Graphics.h"
 
 namespace love
 {
@@ -40,49 +39,24 @@ int w_Canvas_getMSAA(lua_State *L)
 
 int w_Canvas_renderTo(lua_State *L)
 {
-	Graphics::RenderTarget rt(luax_checkcanvas(L, 1));
-
-	int args = lua_gettop(L);
-
+	Canvas *canvas = luax_checkcanvas(L, 1);
 	int startidx = 2;
-
-	if (rt.canvas->getTextureType() != TEXTURE_2D)
+	int slice = 0;
+	if (canvas->getTextureType() != TEXTURE_2D)
 	{
-		rt.slice = (int) luaL_checkinteger(L, 2) - 1;
+		slice = (int) luaL_checkinteger(L, 2) - 1;
 		startidx++;
 	}
-
 	luaL_checktype(L, startidx, LUA_TFUNCTION);
-
-	auto graphics = Module::getInstance<Graphics>(Module::M_GRAPHICS);
-
-	if (graphics)
+	luax_catchexcept(L, [&]()
 	{
-		// Save the current render targets so we can restore them when we're done.
-		Graphics::RenderTargets oldtargets = graphics->getCanvas();
-
-		for (auto c : oldtargets.colors)
-			c.canvas->retain();
-
-		if (oldtargets.depthStencil.canvas != nullptr)
-			oldtargets.depthStencil.canvas->retain();
-
-		luax_catchexcept(L, [&](){ graphics->setCanvas(rt, false); });
-
-		int status = lua_pcall(L, args - startidx, 0, 0);
-
-		graphics->setCanvas(oldtargets);
-
-		for (auto c : oldtargets.colors)
-			c.canvas->release();
-
-		if (oldtargets.depthStencil.canvas != nullptr)
-			oldtargets.depthStencil.canvas->release();
-
-		if (status != 0)
-			return lua_error(L);
-	}
-	
+		auto *module = luax_getmodule(L, Module::M_GRAPHICS);
+		auto *command = dynamic_cast<GraphicsCanvasCommand *>(module);
+		if (command == nullptr)
+			throw love::Exception(
+				"love.graphics has no state-local Canvas command adapter");
+		command->renderTo(L, canvas, slice, startidx);
+	});
 	return 0;
 }
 
@@ -92,13 +66,11 @@ int w_Canvas_newImageData(lua_State *L)
 	love::image::Image *image = luax_getmodule<love::image::Image>(L, love::image::Image::type);
 
 	int slice = 0;
-	int mipmap = 0;
-	Rect rect = {0, 0, canvas->getPixelWidth(), canvas->getPixelHeight()};
-
 	if (canvas->getTextureType() != TEXTURE_2D)
 		slice = (int) luaL_checkinteger(L, 2) - 1;
-
-	mipmap = (int) luaL_optinteger(L, 3, 1) - 1;
+	int mipmap = (int) luaL_optinteger(L, 3, 1) - 1;
+	Rect rect = {0, 0, canvas->getPixelWidth(mipmap),
+		canvas->getPixelHeight(mipmap)};
 
 	if (!lua_isnoneornil(L, 4))
 	{

