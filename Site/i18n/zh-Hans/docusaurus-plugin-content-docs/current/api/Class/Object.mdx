import "@site/src/languages/highlight";

# Object

**描述：**

&emsp;&emsp;被Lua虚拟机管理的C++对象的基类。

**继承自：** ContainerItem。

## id

**类型：** 只读成员变量。

**描述：**

&emsp;&emsp;C++对象的ID。

**签名：**
```tl
const id: integer
```

## ref

**类型：** 只读成员变量。

**描述：**

&emsp;&emsp;C++对象的Lua引用ID。

**签名：**
```tl
const ref: integer
```