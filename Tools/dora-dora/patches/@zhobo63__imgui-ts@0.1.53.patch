diff --git a/src/imgui_impl.ts b/src/imgui_impl.ts
index 4fd90c79c7c47a6447550b96161629b7c0d079a8..7c07f22954f21372a5a3c0a9daf9f522ce25cba2 100644
--- a/src/imgui_impl.ts
+++ b/src/imgui_impl.ts
@@ -764,7 +764,15 @@ function input_text_update(io:ImGui.IO):void {
         dom_input=inp;
     }
     let framebb=inpState.FrameBB;
-    inp.setRect(framebb.Min.x, framebb.Min.y, framebb.Max.x-framebb.Min.x, framebb.Max.y-framebb.Min.y);
+    const rect=canvas?.getBoundingClientRect();
+    const offsetX=rect?.left ?? 0;
+    const offsetY=rect?.top ?? 0;
+    inp.setRect(
+        offsetX+framebb.Min.x,
+        offsetY+framebb.Min.y,
+        framebb.Max.x-framebb.Min.x,
+        framebb.Max.y-framebb.Min.y
+    );
     if(current_input_text!==inp.Text) {
         current_input_text=inp.Text;
         if(inp.Text)
@@ -998,7 +1006,10 @@ export function RenderDrawData(draw_data: ImGui.DrawData | null = ImGui.GetDrawD
                     // gl.ONE
                     // Bind texture, Draw
                     gl && gl.activeTexture(gl.TEXTURE0);
-                    gl && gl.bindTexture(gl.TEXTURE_2D, draw_cmd.TextureId);
+                    if (gl) {
+                        const texture = draw_cmd.TextureId as WebGLTexture | null;
+                        gl.bindTexture(gl.TEXTURE_2D, texture && gl.isTexture(texture) ? texture : null);
+                    }
                     gl && gl.drawElements(gl.TRIANGLES, draw_cmd.ElemCount, idx_buffer_type, idx_buffer_offset);
 
                     if (ctx) {
@@ -1244,6 +1255,7 @@ export function DestroyDeviceObjects(): void {
     const gl2: WebGL2RenderingContext | null = typeof WebGL2RenderingContext !== "undefined" && gl instanceof WebGL2RenderingContext && gl || null;
     if(g_vao) {
         gl2 && gl2.deleteVertexArray(g_vao);
+        g_vao = null;
     }
     gl && gl.deleteBuffer(g_VboHandle); g_VboHandle = null;
     gl && gl.deleteBuffer(g_ElementsHandle); g_ElementsHandle = null;
